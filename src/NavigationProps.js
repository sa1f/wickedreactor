// @flow
'use strict'

// Used because React Navigation is not checked by flow
// Should be extended by props classes in navigation components
export default class NavigationProps {
  navigation: any;
}
